
# recipe ls, get, rm, create
    tck recipe create [name] 
        creates directory and starter recipe.json file with text file created and glob copy 
            // Check if directory exists
            // If not, create It
                // create recipe.json
                // create starter files:
                // start.md - documentation on how to write recipes
            // If does, option to overwrite
    
    tck recipe ls 
        lists all recipes
    tck recipe get --all
         open recipes directory
    tck recipe rm [name] 
        removes recipe. Confirm operation

Make more templates - add repo
    - vault dev
        - Shell scripts with vault server -dev commands
    - vault docker
        - docker run commands in shell
        - docker-compose file
        - docker-compose agent file
    - vault terraform provider
        - main.tf
    - Hvac
        - python project
    - SEV
        - sev notes and scripts
        - call-notes.txt
    - Sentinel
        - sentinel stuff


Readme/Docs

# Walkthrough
Install
Bootstrap
Initialize a Ticket directory
    - Description
Get It
    - Nav option
List tickets
    - Url-format
Templates
    - Initialize with template
    - Template syntax
        - FilesToCreatetc
        - FilesToCopy
Remove a ticket
Close a ticket
/ list/get/rm closed tickets
Re-open a ticket

0.1

Refactor copy

# comma separated recipecs
tck init [ticket] -r=sev,auth
tck add-recipe ticket-id --recipe=sys,vault-dev
    // Optionally overwrite files that already exist

Autocompletion

Docs

0.2
deep copy, copy directories

Backup tickets to s3
Backup recipes to github
    // Recommend https://gitleaks.io/ action
DO NOT backup tickets to Github
Backup to s3
Backup templates to Github or S3